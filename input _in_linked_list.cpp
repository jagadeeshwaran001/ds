#include<iostream>

using namespace std;

class Node
{
    int data;
    node* next;
};
Node * head = new Node();
void list(int d)
{
    int i = 1;
    Node* temp = new Node();
    while(temp != NULL)
    {
        temp->data = d;
        temp = temp->next;
        if(i == 1):
        {
            
        }
    }
}